/**
 * @deprecated Import from `components/ui/Toast` directly in new code.
 * Kept as a thin re-export so existing call sites keep working without
 * maintaining two divergent toast implementations.
 */
// eslint-disable-next-line react-refresh/only-export-components -- compatibility re-export
export { default, type ToastState } from './ui/Toast';
